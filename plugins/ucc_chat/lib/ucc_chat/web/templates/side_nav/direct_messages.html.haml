%ul.nav
  %li{class: "add-room background-transparent-darker-hover#{is_active(@rooms)}"}
    %a.heading
      - if @account.chat_mode do
        %p
          = ~g(RECENT)
          %span.room-count-small= "(#{length @rooms})"
      - else
        %p
          = ~g(DIRECT MESSAGES)
          %span.room-count-small= "(#{length @rooms})"
  - if @rooms != [] do
    - for room <- @rooms do
      = render "chat_room_item.html", room: room
  - else
    %li.nav-text
      %a
        %p= ~g(No direct messages yet)

  %li
    %a.more.more-channels.background-transparent-darker-hover
      %p= ~g(More Users...)
