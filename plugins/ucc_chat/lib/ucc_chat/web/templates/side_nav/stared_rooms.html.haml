%ul.nav
  %li{class: "#{is_active(@rooms)}"}
    %a.heading
      %i.icon-star
      %p
        FAVORITES
        %span.room-count-small= "(#{length @rooms})"
  - for room <- @rooms do
    = render "chat_room_item.html", room: room
